"""Shared core for the Stream Deck toolkit — the pieces the icon-pack tool
(`sdicons`) and the plugin tool (`sdplugin`) both use: coloured output, the
`Finding` model + report, and the container (zip) verify helper.
"""
